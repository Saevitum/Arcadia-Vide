--!strict

return {
	ActionButton = require(script.ActionButton),
	Text = require(script.Text),
	Image = require(script.Image),
	Slider = require(script.Slider),
	ToggleButton = require(script.ToggleButton),
	ToggleSwitch = require(script.ToggleSwitch),

	Background = require(script.Background),
	ExitButton = require(script.ExitButton),
	Header = require(script.Header),
	Panel = require(script.Panel),
	ScrollArea = require(script.ScrollArea),
}
